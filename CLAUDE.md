# CLAUDE.md

pptx2pdf — pptx を PDF に変換するコマンド（実 PowerPoint / LibreOffice / 任意のツール）。
利用者向けの説明は [README.md](README.md)。本ファイルはこのリポジトリで作業する際の運用メモ。

もとは [md2pptx](https://github.com/toshi0806/md2pptx) の `md2pptx/pdf.py` と
`md2pptx/workdir.py`。**コード中の Issue 番号は md2pptx リポジトリのもの**で、
設計の経緯（#39 / #44 / #46 / #48 / #53 / #58）はそちらに残っている。

## 構成

```
pptx2pdf/converter.py  変換の本体（探索・起動・待ち・差し替え）
pptx2pdf/workdir.py    使い捨て作業ディレクトリの作成と片付け
pptx2pdf/msg.py        利用者に見える名前（プログラム名・フラグ名）
pptx2pdf/cli.py        コマンドの入口
```

**依存は標準ライブラリだけ**。python-pptx を持ち込まないこと——pptx の中身を読まないから
こそ、md2pptx のような生成ツールからも、手元の pptx を変換するだけの用途からも使える。

## 規約・設計上の約束

**在る物の失敗は握らない**（md2pptx Issue #46）。`auto` は「使えるものを探す」だけで、
選んだ変換器が失敗したら次へは落とさない。落とすと忠実度の違う PDF を黙って掴むことになり、
しかも隠れる原因（オートメーション承認の拒否、ライセンス未認証）は利用者が直せるもの。
`_Unavailable`（＝この環境に無い）だけを握って次へ進む。**`except _Unavailable` は
`except PdfError` より必ず先に置くこと**（サブクラスなので、入れ替えると #46 の挙動が
黙って戻る）。固定しているのは `tests/test_pdf_converter_choice.py`。

**変換前に既存 PDF を消す実装に戻してはいけない**。PDF ビューアはフォルダを監視していて、
削除を確定するとそのファイルを監視から外す（LaTeX Workshop は 250ms で確定し、集合が
空になるとフォルダごと破棄）。変換には1〜数秒かかるので必ず確定してしまい、
**編集しながらのプレビューが最初のリビルドで死ぬ**（実測: 修正前はリビルド1回あたり約1秒
PDF が不在／修正後は0）。作業場所がファイルではなくディレクトリなのは、LibreOffice が
`--outdir` に `<入力 basename>.pdf` を書くため（`slide.pptx` → `slide.pdf` では出力 PDF を
直接書いてしまう）。無音失敗の検出（存在＋非空）は削除ではなく「毎回まっさらな別名へ
書かせる」ことで担保している。固定しているのは `tests/test_pdf_replace.py`。

**使い捨て作業ディレクトリの作成と片付けは `workdir.py` に集約する**（Issue #58）。
規則は2つで、どちらも外すと壊れる: **①片付けの失敗で処理の成否を変えない**（片付けに
入る時点で仕事は終わっている。投げると成功した実行が失敗になり、本体の例外があれば
それを置き換えてしまう）、**②それでも黙って残さない**（監視ループでは変換のたびに
1つずつ溜まり、出力先のものは利用者の目に触れる）。`tempfile.TemporaryDirectory` に
戻してはいけない——**片付けの失敗で例外を投げる**（既定 `ignore_cleanup_errors=False`）ので
① が崩れる。固定しているのは `tests/test_workdir.py`。

**待ちの上限は tty かどうかで分ける**（Issue #48）。tty なら打ち切らない——止まる原因の
多くは承認ダイアログのような「人が今すぐ直せるもの」で、30秒の案内はそれを直して
もらう仕掛けだから、上から kill を被せると自分で用意した解決手段を潰すことになる。
非 tty（cron / CI / エディタ拡張）は180秒で打ち切る。`--timeout` / `PPTX2PDF_TIMEOUT`
で上書き（`0` は無制限）。打ち切り時は自分で起こした子プロセスだけを kill し、書きかけの
PDF を消す。`convert(..., unattended=True)`（CLI では `--unattended`）はこの tty からの
推測を呼び出し側が明示的に打ち消す入口（上限と前面化の両方に効く）。固定しているのは
`tests/test_pdf_timeout.py`。

macOS 経路は **PowerPoint を目立たせずに使う**（Issue #44）。仕組みは2つで、どちらも
消すと運用が壊れる:

- `activate` を入れず `open -g -j -a` で非表示・非アクティブ起動してから文書を開く。
  ウィンドウが出るのは**利用者が既に PowerPoint を表示して使っている場合だけ**で、
  これは仕様（`-j` は起動の瞬間にしか効かない）。`save … as PDF` は隠したアプリを自ら
  再表示するので、起動後に隠し直す方法では抑えられない（測定済み）。
- 変換は **PowerPoint のサンドボックスコンテナ内**で行う
  （`~/Library/Containers/com.microsoft.Powerpoint/Data/tmp`）。pptx をそこへコピーして
  変換し、PDF を目的地へ移す。未承認の場所を直接開かせるとファイルアクセスの許可
  ダイアログ待ちで止まるが、隠していると利用者にはそれが見えないため（実測:
  未承認フォルダ25秒でタイムアウト／コンテナ内 1.1 秒で成功）。

隠したことで気づけない停止（オートメーション承認など）に備え、30秒で stderr に案内を
出す（前面化は tty かつ `attended` のときだけ）。

**入力を複数取るときも、1つの失敗で残りを捨てない**。`pptx2pdf *.pptx` に壊れた
ファイルが1つ混じっていただけで後ろが丸ごと変換されないと、何が終わって何が終わって
いないのか分からなくなる。失敗は名前と理由を stderr に出して次へ進み、最後に失敗数を
伝えて終了コード1にする。**入力が1つのときのメッセージは変えない**（どのファイルの
話かは自明で、名前を足すとスクリプトが読んでいる文言が動く）。固定しているのは
`tests/test_cli.py`。

**利用者に見える名前は `msg.py` から取る**。このパッケージは単体のコマンドとしても、
別のツールの一部としても動く（md2pptx の `--pdf` はここを呼ぶ）。`md2pptx deck.md --pdf`
の途中で `pptx2pdf: warning: …` と出ても、利用者はそんなコマンドを打っていない。
案内するフラグ名も同じ理由で差し替え可能にしてある（`--timeout` ↔ `--pdf-timeout`）。
参照は `msg.PROG` のように**属性で**行うこと（`from .msg import PROG` は差し替え前の値を
焼き付ける）。作業ディレクトリの接頭辞もここから作る（利用者の目に触れる名前だから）。

## コマンド

```bash
pytest          # 単体テスト（外部プロセスは起こさない）
mypy            # 型チェック
python3 -m pptx2pdf slide.pptx     # インストールせずに実行
```

CI（`.github/workflows/ci.yml`）は3ジョブ。`typecheck` が mypy を1回、`test` が
**3.11 と 3.14 のマトリクス**で pytest、`convert` が LibreOffice を入れて実際の変換
（既定の出力先・`-o` 指定・変換器が無いときの失敗）まで検証する。runner に PowerPoint は
無いので **LibreOffice 経路のみ**。macOS の実 PowerPoint 経路は CI では踏めず、手元での
確認になる。

## 落とし穴

- **モジュール名と関数名を衝突させない**。`__init__.py` が `convert` 関数を再公開して
  いるので、実装モジュールを `convert.py` にすると `from pptx2pdf import convert` が
  関数を返し、モジュールを差し替えるテストが書けなくなる。実装は `converter.py`。
- **変換の無音失敗を終了コードだけで判定しない**。macOS の `save … as PDF` は exit 0 でも
  PDF を作らないことがある。存在＋非空を成功条件にしている。
- **壊れた pptx を PowerPoint に渡すと、以後の変換まで落ちる**（実測）。開けなかった
  ファイルのダイアログを抱えたままになり、次の変換は `-9074` や AppleEvent の
  タイムアウトで失敗する。**同じプロセス内の話ではない**——別々に `pptx2pdf` を
  起動しても同じで、`quit saving no` すら `-128` で拒否された。隠して動かしている
  以上これは画面に見えないので、README に「前面に出して答えるか終了させる」と
  書いてある。ここで PowerPoint を殺す実装を足してはいけない（利用者が開いて
  使っているインスタンスかもしれない）。
- 各サブプロセス/Bash 間で `/tmp` の状態が保持されないことがある。
  生成→検証は1コマンド内で完結させると安全。

## このリポジトリについて

- リモート `origin` は https://github.com/toshi0806/pptx2pdf 。
- コミットメッセージは英語。PR ワークフローの規約は md2pptx と同じ。
- 文書の日本語は、描画結果が変わらない位置でだけ折る（句読点の直後、または既に空白が
  ある日英の境目）。`**` は記号に接して閉じられない（`**…）**で` は強調にならない）。
